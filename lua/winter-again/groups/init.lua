local M = {
    base = require("winter-again.groups.base"),
    blink = require("winter-again.groups.blink"),
    -- cmp = require("winter-again.groups.cmp"),
    fzf = require("winter-again.groups.fzf"),
    gitsigns = require("winter-again.groups.gitsigns"),
    hlsearchlens = require("winter-again.groups.hlsearchlens"),
    incline = require("winter-again.groups.incline"),
    indent_blankline = require("winter-again.groups.indent-blankline"),
    lazy = require("winter-again.groups.lazy"),
    lsp = require("winter-again.groups.lsp"),
    -- mason = require("winter-again.groups.lsp"),
    -- nvim_tree = require("winter-again.groups.nvim-tree"),
    oil = require("winter-again.groups.oil"),
    statusline = require("winter-again.groups.statusline"),
    syntax = require("winter-again.groups.syntax"),
    -- telescope = require("winter-again.groups.telescope"),
    todo_comments = require("winter-again.groups.todo-comments"),
    trouble = require("winter-again.groups.trouble"),
    treesitter = require("winter-again.groups.treesitter"),
    treesitter_context = require("winter-again.groups.treesitter-context"),
}

return M
